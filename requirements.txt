# C-GRPO — pinned to the versions the experiments were run with (Python 3.10.19)
# Install torch first, matched to your CUDA build:
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121

torch==2.5.1
transformers==5.1.0
peft==0.18.1
datasets==4.5.0
accelerate==1.12.0
safetensors==0.7.0
numpy==1.26.4
tqdm==4.67.3
tensorboard==2.20.0

# optional: experiment tracking (the code runs fine without it)
# wandb>=0.16
